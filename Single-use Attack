# cook your dish here
import math
t = int(input())
for _ in range(t):
    H, X, Y = map(int,input().split())
    rem = H - Y
    if rem <= 0:
        print(1)
    else:
        n = math.ceil(rem / X)
        print(1 + n)
