# cook your dish here
t = int(input())
for _ in range(t):
    W, X, Y, Z = map(int, input().split())
    possible = {
        X,
        Y,
        Z,
        X + Y,
        X + Z,
        Y + Z,
        X + Y + Z
    }
    if W in possible:
        print("YES")
    else:
        print("NO")
