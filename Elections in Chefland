# cook your dish here
T = int(input())
for _ in range(T):
    N, X = map(int, input().split())
    ages = list(map(int, input().split()))
    count = 0
    for age in ages:
        if age >= X:
            count += 1
    print(count)
