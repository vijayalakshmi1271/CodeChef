# cook your dish here
for _ in range(int(input())):
    x,y = map(int,input().split())
    if x > y:
        t = x-y
        
    elif x == y:
        t =0
    else:
        t = y-x
    
    print(t)
